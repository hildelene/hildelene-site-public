import { EquipeCarousel, ConquistasCarousel } from './MainCarousel';

export default function EquipeConquistasCarousels() {
  return (
    <section id="main-carousels" className="main-carousels-section">
      <EquipeCarousel />
      <ConquistasCarousel />
    </section>
  );
}
